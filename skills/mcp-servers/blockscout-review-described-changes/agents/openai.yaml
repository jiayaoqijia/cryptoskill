interface:
  display_name: "Review Described Changes"
  short_description: "Critically verify a proposed change overview"
  default_prompt: "Use $review-described-changes to read the issue, inspect the described changes overview, open the referenced code, and report gaps, inconsistencies, and likely mistakes without modifying any files."
policy:
  allow_implicit_invocation: false
