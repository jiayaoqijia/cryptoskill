interface:
  display_name: "Plan Export"
  short_description: "Export phased implementation plans for issues"
  default_prompt: "Use $plan-export to create a detailed phased implementation plan for a GitHub issue, following the skill's planning, file, and verification requirements."
policy:
  allow_implicit_invocation: false
