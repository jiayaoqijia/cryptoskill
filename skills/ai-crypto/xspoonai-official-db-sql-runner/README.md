# db-sql-runner (Track: ai-productivity)

Run parameterized SQL against a configured DB (dev only)

## Quickstart
```
python3 scripts/main.py --help
```

## Example
```
python3 scripts/main.py --demo
```
