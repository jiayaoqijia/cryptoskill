# Example Usage

This folder contains a minimal demo for the database migration assistant.

## Quick CLI Demo

1. Set DB_URL (see example.env).
2. Generate a migration template.
3. Run plan, apply (dry-run), apply, then rollback.

See README.md for full commands.
